# OPM (OpenProxy MCP)

이 저장소의 에이전트 지침은 **`CLAUDE.md` 하나**가 정본이다. 규칙·명령·구조·워크플로 전부 그 파일을 읽는다.
여기에 내용을 복제하지 않는다 — 두 벌이면 어긋난다(260902 통합).

보고는 `CLAUDE.md` 「보고」 절을 따른다. 결론 한 줄 먼저, 우리말로, 숫자는 표에, 못 한 것 먼저.
