# 260906 — 공시 원문 절 단위 리소스(`toc`·`section`) 실측 핸드오프

**상태**: 설계 검토·리모트 실험까지 완료, **뷰어 실측은 로컬에서 해야 함**(원격 샌드박스가 dart.fss.or.kr 을 403 으로 막는다). 코드 변경 없음(계측 스크립트만 추가).

## 1. 지금까지 확인된 것

### 원문 리소스는 지금 답을 못 준다 (리모트 실측, 삼성전자 2025 사업보고서 `20260310002820`)
| 측정 | 값 |
|---|---|
| document.xml 전체 | 820,256자 |
| `opm://filing/{rcept_no}` 가 준 양 | 120,019자 (15%) — `_MAX_CHARS` 절단 |
| 준 범위 | I. 회사의 개요(454) → II. 사업의 내용(27,183) → III. 재무에 관한 사항(67,458)에서 잘림 |
| 「직원 현황·1인평균 급여」(VIII장) | 잘린 뒤 — 못 닿음 |
| 정제 텍스트에서 잡히는 헤딩 | 장(I·II·III) + 절(`1.`·`가.`) 175개 |

→ 파서 없는 질문 대부분이 III장 이후에 있고 거기가 통째로 잘린다. 답을 못 찾으면서 12만 자를 소비하는 구조.

### 절 단위 읽기의 밑바닥은 이미 있다 (`open_proxy_mcp/dart/client.py`)
- `_fetch_viewer_main_html(rcept_no)` → DART 뷰어 main.do (웹, 키 미사용, `_throttle_web`)
- `_extract_viewer_nodes(html)` → 뷰어 목차 treeData 에서 `text·tocNo·eleId·dcmNo` 절 목록
- `_fetch_viewer_section_html(node)` → 절 하나의 HTML
- `get_viewer_document(rcept_no, section_keywords)` → 키워드로 절 골라 text+html+nodes, `_doc_cache` 캐시
- 쓰는 곳: `services/business_details.py`(II. 사업의 내용 하위 절), `tools/financial_notes.py`(주석 절). 둘 다 특정 절 고정 내부 호출. **아무 공시의 아무 절**을 밖에서 고르는 창구가 없다.

### 260813 「Claude.ai 커넥터는 resource 를 모델에게 노출하지 않는다」는 뒤집혔다
이 세션(claude.ai 원격 커넥터)에서 `opm://tools_guide` 를 URI 로 직접 읽어 전문이 돌아왔다. 단 `resources/list` 는 커넥터가 접속 시점 스냅샷을 잡아 새 리소스가 목록에 안 보인다 — **URI 를 아는 경우에만 읽힌다.** 따라서 「도구 결과가 URI 를 글자로 적어 주고 그걸 읽는」 경로가 실제로 작동하는 경로다.
정정 대상: `wiki/tools/proxy_guideline.md:26` · `wiki/decisions/open-proxy-guideline.md:40` · `docs/RELEASE_NOTES.md:122`(ENG 135).

### 프롬프트 `claim_check` 는 보류 권고
「삼성전자 정규배당 연 9.8조 정책대로 집행」 주장을 기존 `dividend_disclosure` 출력만으로 지지(비고 원문 9.8조·분기 2.45조)/반박(1.3조 추가해 총 11.11조)/확인 불가(1.3조의 성격) 로 가를 수 있었다. 프롬프트가 보태는 건 데이터가 아니라 규율이고, 그건 서버 instructions 한 줄로 된다. 이 세션 커넥터엔 프롬프트를 고를 UI 도 없다.

## 2. 로컬에서 할 것 — 뷰어 실측

```bash
# 표본 절만(직원·주석·사업의 내용·배당·주주) — 절당 1~2초
uv run python scripts/probe_viewer_sections.py 20260310002820
# 전 절 — 절 수 × 1~2초. 대형·중형·금융사 하나씩
uv run python scripts/probe_viewer_sections.py 20260310002820 --all --json /tmp/probe_samsung.json
uv run python scripts/probe_viewer_sections.py <중형사 사업보고서 rcept_no> --all --json /tmp/probe_mid.json
uv run python scripts/probe_viewer_sections.py <은행·보험 사업보고서 rcept_no> --all --json /tmp/probe_fin.json
```
`.env` 에 `OPENDART_API_KEY` 가 있어야 한다(클라이언트 생성자가 요구. 뷰어 호출은 키를 안 쓴다). CLAUDE.md 규칙 7 대로 절을 순서대로 하나씩 받는다 — 병렬 금지.

### 무엇을 볼지
| 항목 | 왜 |
|---|---|
| 절 수 | 목차 리소스 크기. 사업보고서는 40 안팎일 것 |
| 절별 텍스트 글자 수 분포 · 최대(재무제표 주석) | 절 리소스 상한(4만 자 안)과 이어읽기(`from=`) 필요 여부 |
| 절별 HTML 바이트 · `표` 수 | md 표 변환 비용, 단위·각주가 절 안에 같이 오는지 |
| `캐시 B(html+text)` vs `(text)` | 절을 캐시할 때 html 을 버릴지 |
| 전 절 캐시 MB / 96MB | 문서 하나가 캐시 예산에서 차지하는 비율 |
| peak RSS 시작→끝 | 전 절을 순서대로 받았을 때 프로세스가 얼마나 부푸나 |
| 실패 절 | 뷰어가 절 HTML 을 안 주는 유형(첨부·XBRL) |

### 판단 기준(가안)
- 절 평균 4만 자 이하, 주석 절이 그보다 크면 `from=` 이어읽기를 첫 구현에 포함.
- 문서 하나 전 절 캐시가 10MB(예산의 ~10%) 넘으면 절 캐시는 **텍스트만** 저장.
- 절당 왕복 2~3초면 「절 하나씩」 설계로 충분. 그보다 느리면 목차에 절 크기를 미리 실어 선택을 돕는다.

### 실측 결과 (260906 로컬 · 사업보고서 2025.12 3건 · 절 순차 1개씩)

| 표본 | rcept_no | 시장 |
|---|---|---|
| 대형 | 삼성전자 `20260310002820` | KOSPI |
| 중형 | 리노공업 `20260318000182` | KOSDAQ |
| 금융 | 삼성화재해상보험 `20260312001399` | KOSPI 보험 |

**스크립트 그대로 돌린 결과는 「장(章)」 단위다.** `_extract_viewer_nodes` 가 `var node1` 블록만 잡아서 3건 모두 15절(I~XII 장 + 표지·확인서)만 나왔다. 실제 뷰어 목차는 3계층(node1 장 › node2 절 › node3 항)이고, `services/business_details.py:_extract_node_tree` 가 이미 전 계층을 푼다. 그래서 같은 측정을 전 계층으로 다시 했다(스크래치 스크립트, 레포 밖 · 계측 로직은 동일 · `<table` 은 뷰어가 대문자라 대소문자 무시로 셈).

#### A. 장 단위 (`scripts/probe_viewer_sections.py --all`)
| | 삼성전자 | 리노공업 | 삼성화재 |
|---|---|---|---|
| 목차 절 수 | 15 | 15 | 15 |
| main.do | 112KB · 0.21s | 90KB · 0.19s | 122KB · 0.20s |
| 문서 전체 텍스트 | 818,405자 | 168,311자 | 1,083,184자 |
| 최대 장 (III. 재무) | 280,857자 · HTML 1.8MB | 82,670자 · 0.5MB | 722,441자 · 5.3MB |
| 전 장 캐시 html+text | 9.56MB (예산 10.0%) | 1.93MB (2.0%) | 14.03MB (14.6%) |
| 전 장 캐시 text | 1.67MB | 0.36MB | 2.20MB |
| 절당 왕복 | 0.95~2.03s | 0.98~1.85s | 0.3~3.01s |
| peak RSS 시작→끝 | 44.0 → 86.9MB | 42.4 → 58.6MB | 43.4 → 124.5MB |
| 실패 절 | 0 | 0 | 0 |

#### B. 전 계층 (leaf 절 = 하위가 없는 노드)
| | 삼성전자 | 리노공업 | 삼성화재 |
|---|---|---|---|
| 노드 수 (장/절/항) | 134 (15/43/76) | 95 (15/42/38) | 151 (15/40/96) |
| leaf 절 수 | 120 | 83 | 137 |
| 목차 캐시 (no·level·text) | 43KB | 31KB | 49KB |
| leaf 텍스트 평균 / 중앙 / 최대 | 6,815 / 2,678 / **124,752** | 2,023 / 1,182 / 14,697 | 7,902 / 3,435 / **100,630** |
| leaf > 40,000자 | 4 | 0 | 3 |
| leaf > 20,000자 | 9 | 0 | 13 |
| leaf 캐시 text / html+text | 1.67 / 10.16MB | 0.36 / 2.12MB | 2.20 / 14.87MB |
| 전 노드 캐시 text / html+text | 3.66 / 22.44MB | 0.83 / 5.01MB | 5.51 / 38.43MB |
| leaf `<table>` 합 · 절당 최대 | 2,073 · 74 | 907 · 59 | 3,452 · 186 |
| 절당 왕복 평균 / 최대 | 1.52 / 2.01s | 1.49 / 2.00s | 1.53 / 2.21s |
| 전 노드 순차 소요 | 203s | 142s | 231s |
| peak RSS 시작→끝 | 51.3 → 81.8MB | 51.5 → 53.0MB | 51.5 → 120.0MB |
| 실패 절 | 0 | 0 | 0 |

40,000자 넘는 leaf: 삼성전자 「VIII-1. 임원 및 직원 등의 현황」 124,752 · 「IX. 계열회사」 56,396 · 「XII-1. 연결대상 종속회사(상세)」 45,478 · 「XII-4. 연구개발실적(상세)」 67,873 / 삼성화재 「IX. 계열회사」 100,630 · 「주석 5. 보험계약 위험관리」 연결 72,023 · 별도 72,018. 주석 항 단위는 삼성전자 최대 「31. 특수관계자와의 거래」도 4만 자 안이다.

#### 관찰
1. **viewer.do 는 부모 노드를 부르면 하위를 통째로 준다.** 모든 부모에서 `부모 글자 수 ≈ 자식 합 + 제목 몇십 자`(삼성전자 III 장 280,857 vs 자식 8개 합 280,818; 삼성화재 연결주석 340,811 vs 항 45개 합 340,662). → 절 리소스는 **leaf 를 기본**으로 하고 부모 번호를 받으면 하위 목록으로 안내하거나 leaf 를 이어 붙여야 한다. 부모를 그대로 받으면 금융사 III 장 하나가 5.3MB HTML 이다.
2. **RSS 피크는 부모 노드 수신에서 났다.** 삼성화재 +66MB 는 III 장(5.3MB HTML) 한 번, 삼성전자 +21MB 도 III 장. leaf 만 받는 별도 프로세스 실측: 최대 leaf 3개(IX 계열회사 496KB · 보험계약 위험관리 505KB/표 186 · 계열회사 상세 196KB) 순차 수신에 51.3 → 60.0 → 64.2 → 65.5MB, 즉 **leaf 요청 하나의 피크 ≤ 9MB**. `doc_gate`(동시 2) 안에서 충분하다.
3. **캐시는 text 만.** html 은 text 의 5~7배(삼성화재 leaf 14.87MB vs 2.20MB). 한 문서 leaf 전부를 text 로 캐시해도 예산 96MB 의 2.3% 이하.
4. **왕복은 절 크기와 무관하게 1.5초 안팎** — `_throttle_web` 간격(1~2초)이 지배하고 네트워크는 0.2~0.3초. 「절 하나씩」 설계로 충분.
5. **실패 절 0.** 사업보고서 3건 모두 첨부·XBRL 노드가 목차에 없어 실패 유형은 못 봤다(첨부는 별도 rcept_no). 소집공고 같은 다른 서식은 미측.
6. 표는 절 안에 단위·기준일이 같이 온다(예: 「(기준일 : 2025년 12월 31일) (단위 : 주)」가 표 앞 셀). 표당 셀이 줄바꿈으로 흩어져 md 표 변환은 필요.

#### §2 판단 기준 대조
| 기준 | 결과 | 판정 |
|---|---|---|
| 절 평균 4만 자 이하 | leaf 평균 2~8천 자 · 중앙 1~3천 자 | 통과 |
| 주석 절이 4만 자보다 크면 `from=` 포함 | 주석 **항**은 4만 자 안이나 「임직원 현황」·「계열회사」·「보험계약 위험관리」 등 leaf 7개가 4.5만~12.5만 자 | **`from=` 이어읽기를 첫 구현에 포함** |
| 문서 전 절 캐시 10MB 넘으면 text 만 | html+text 로는 10.2·14.9MB 초과, text 만은 최대 2.2MB | **text 만 저장** |
| 절당 왕복 2~3초면 「절 하나씩」 | 평균 1.5초 · 최대 3.0초 | 통과 — 다만 목차에 절 크기(`toc_length` 또는 캐시된 글자 수)를 실어 큰 절을 미리 알린다 |

**착수 가능.** 단 설계에 반영할 세 가지: ① 목차·절은 `_extract_node_tree`(전 계층) 기준, `no` 는 `tocNo`. ② 절 리소스는 leaf 기본 + 부모 번호는 하위 안내(또는 명시 옵션으로 병합) — 부모를 그대로 주면 5MB 를 받는다. ③ 캐시는 절별 text 만, `from=` 은 첫 구현에.

계측 스크립트 손볼 것(코드 변경 허가 후): `_extract_viewer_nodes` 대신 `_extract_node_tree` 사용, `<table` 대소문자 무시, leaf 표시. 원자료: 세션 스크래치 `probe_*.json`(장 단위) · `all_*.json`(전 계층).

### 추가 표본 8건 (260906 · 섹터·시장 확장 · 전 계층 · 절 순차)

앞의 3건에 은행지주·증권·건설·바이오·유통·리츠·코스닥 소형·공기업을 더해 11건.

| 문서 | 시장·섹터 | 노드 / leaf | leaf 중앙 / 최대(자) | leaf>4만 | leaf 캐시 text / html+text(MB) | peak RSS(MB) | 실패 |
|---|---|---|---|---|---|---|---|
| 삼성전자 | KOSPI 제조 | 134 / 120 | 2,678 / 124,752 | 4 | 1.7 / 10.2 | 51 → 82 | 0 |
| 리노공업 | KOSDAQ 중형 | 95 / 83 | 1,182 / 14,697 | 0 | 0.4 / 2.1 | 52 → 53 | 0 |
| 삼성화재 | KOSPI 보험 | 151 / 137 | 3,435 / 100,630 | 3 | 2.2 / 14.9 | 52 → 120 | 0 |
| KB금융 | KOSPI 은행지주 | 172 / 158 | 2,992 / 228,859 | 11 | 3.3 / 26.0 | 52 → 161 | 0 |
| 미래에셋증권 | KOSPI 증권 | 169 / 154 | 2,935 / 165,840 | 5 | 2.6 / 19.5 | 52 → 118 | 1 |
| 현대건설 | KOSPI 건설 | 139 / 125 | 2,651 / 31,094 | 0 | 1.3 / 10.8 | 52 → 104 | 0 |
| 알테오젠 | KOSDAQ 바이오 | 145 / 131 | 1,558 / 25,629 | 0 | 0.7 / 5.1 | 52 → 72 | 0 |
| 이마트 | KOSPI 유통 | 157 / 143 | 2,906 / 38,098 | 0 | 1.4 / 10.1 | 50 → 104 | 0 |
| SK리츠 | KOSPI 리츠 | 123 / 108 | 878 / 24,524 | 0 | 0.5 / 3.5 | 52 → 55 | 1 |
| 동운아나텍 | KOSDAQ 소형 | 61 / 50 | 764 / 57,057 | 1 | 0.2 / 1.6 | 50 → 59 | 0 |
| 한국전력 | KOSPI 공기업 | 163 / 149 | 3,366 / 213,855 | 8 | 3.0 / 21.9 | 52 → 156 | 0 |

11건 합계: leaf 1358개 · 4만 자 초과 32개(2.4%) · 실패 2개(0.15%) · leaf 중앙 2,316자 · 평균 6,282자

- **문서별 성공 비중**: 11건 중 9건 100%, 2건은 절 하나씩 실패(미래에셋증권 「44. 예수유가증권」 · SK리츠 「3. 타법인출자 현황(상세)」). 둘 다 **재시도에서 정상 수신**(434자·953자) — 일시 오류. 절 리소스는 재시도 1회 + 실패 시 「본문 미수신 + 다음 절 URI」 안내로 충분하다. 이미지 전용 절·빈 절은 이번 표본에서 안 나왔다(사업보고서 서식은 본문이 텍스트).
- **4만 자 초과 leaf 는 금융·공기업에 몰린다**: KB금융 11 · 한국전력 8 · 미래에셋 5 · 삼성전자 4 · 삼성화재 3 · 동운아나텍 1(주석이 항 없이 절 하나). 최대는 「7. 증권의 발행을 통한 자금조달」(KB 228,859 · 한전 213,855 · 미래에셋 165,840) 과 「종속·관계기업 투자」(한전 165,704). `from=` 이어읽기는 첫 구현 필수(재확인).
- **부모 노드 = 자식 합** 은 11건 전부 성립. RSS 피크는 전부 III 장(부모) 수신에서 났다 — 한전 +83MB(HTML 7.8MB) · KB +55MB(7.1MB) · 현대건설 +45MB(3.3MB). leaf 만 받으면 리츠·소형·바이오는 피크 상승 3~8MB 이내.
- 왕복은 11건 모두 절당 평균 1.45~1.53초 — 문서·절 크기와 무관, 시계 간격이 지배.
- 캐시 text 만: 문서당 0.2~3.3MB. html 까지 넣으면 최대 26MB(KB) — text 만 저장 재확인.

### 운영 VM 메모리 현황 (260906 19:10 live `/health`)
| 항목 | 값 |
|---|---|
| VM | 1,024MB · shared-cpu 1코어 (fly.toml) |
| RSS 현재 / 피크 | 350MB / 426MB |
| 문서 캐시 | 51MB / 96MB (50.8%) · 19건 · 거부 0 |
| doc_gate | 동시 2 · 키당 1 · 사용 0 · 대기 0 |
| 디스크 캐시 예산 | 640MB (`OPM_DOC_DISK_CACHE_MB`) |

→ 피크 기준 여유 ≈ 600MB. leaf 요청 피크 ≤ 9MB × doc_gate 2 = 20MB 안. 부모 노드를 그대로 받으면 요청당 +45~83MB 라 둘이 겹치면 피크가 560MB 근처로 오르고, 파이썬은 피크를 기준선으로 남긴다(현재 RSS 350 vs 피크 426 이 그 흔적). **leaf 기본**이 메모리 쪽에서도 필수. 절 text 를 디스크 캐시(640MB)에 두면 메모리 LRU 는 목차만 들고 갈 수 있다 — 설계 선택지로 추가.

## 3. 메모리 점검(코드 기반, 260906)
- **OOM 이력(260901)은 캐시가 아니라 RSS 였다.** 두 머신이 exit 137 로 죽었을 때 캐시 점유는 296MB 중 33%. VM 1,024MB(fly.toml). 이후 `doc_gate`(문서 수신·파싱 동시 상한), 키별 클라이언트 등록부 상한, `/health` 에 `mem`·`doc_gate` 노출. → 새 리소스는 **반드시 `doc_gate` 를 지나야** 하고, 요청당 피크(뷰어 HTML 파싱)가 관건이다.
- 캐시 계정은 `_cache_entry_bytes`(getsizeof 재귀)라 문자열 지배 페이로드에 정확. 한 항목이 예산(96MB)보다 크면 캐시를 거부하고 로그를 남긴다.
- `get_viewer_document` 는 `text`+`html`+`nodes` 를 같이 캐시한다 — html 이 text 의 2~3배라 **한 문서가 두 벌**로 들어간다. 키워드 없이 부르면 **전 절**을 받아 합친다 → 리소스 경로에서 이 함수를 키워드 없이 호출하면 안 된다.
- `opm://filing/{rcept_no}`(현행)는 `get_document_cached` 로 document.xml 전문을 캐시(82만 자 ≈ 1.6~3MB str)하고 12만 자만 자른다. 메모리보다 **답을 못 주는 것**이 문제.
- 설계 제한: 목차는 `nodes` 만 캐시(수 KB) · 절은 절 단위로 캐시하되 텍스트만 · 절당 텍스트 상한 4만 자 + `from=` · 요청 하나에 절 하나 · 기존 `_throttle_web` 그대로.

## 4. 설계안 (채팅에서 합의된 형태)
```
opm://filing/{rcept_no}                 (있음) 머리에 「목차는 …/toc」 안내를 붙인다
opm://filing/{rcept_no}/toc             신규 — 뷰어 목차 그대로(no=tocNo · 절 제목 · section URI · 크기)
opm://filing/{rcept_no}/section/{no}    신규 — 절 하나. 머리: 상위 장 › 절 제목 · 접수번호 · 글자 수 · 이전/다음 절 URI
                                        본문: 텍스트 + md 표(단위·각주 보존, financial_notes 의 표 변환 재사용)
opm://reading/{topic}                   나중 — 오독 사례 모은 뒤 (basis · absence · segments)
```
도구 → 리소스 연결은 md 본문에 URI 를 글자로 적는다(`business_details` 의 「원문 위치」 줄, 약한 파싱 경고 줄). SDK(mcp 2.0.0)에 `ResourceLink`·`EmbeddedResource` 가 있지만 도구 31개가 전부 str 반환이라 링크 객체는 뒤로 미룬다.

CLAUDE.md 규칙 2(document.xml 우선·뷰어는 service 명시분만)·7(스크래핑 1~2초·배치 금지)은 리소스 PR 에서 「뷰어 절 읽기는 리소스 경로에서 허용, 제한은 §3」로 함께 고친다. CLAUDE.md 전반 정리는 별건(사용자 지시 260906).

## 5. 진행 (260906)
1. ✅ 실측 11건(§2) — 착수 판정.
2. ✅ 구현: `services/filing_sections.py`(목차·절·렌더) · `resources.py`(`/toc`·`/section/{no}{?start}` 등록, 전문 머리에 목차 안내) · `dart/client.py` `doc_gate_slot()`(document.xml 경로와 **같은 문**을 컨텍스트 매니저로 공유) · `tests/test_filing_section_resources.py`(합성 뷰어 fixture, MCP `read_resource` 경로, network 0, 11건) · 릴리즈 노트 한/영 · README 한/영 · 260813 기록 정정(`wiki/tools/proxy_guideline.md`·`wiki/decisions/open-proxy-guideline.md`·`docs/RELEASE_NOTES*.md`). pilot MCP 실측: 삼성전자 목차 0.23s, 절 118(12.9만 자) 1.4s 로 첫 4만 자 + 이어읽기, 부모 절은 뷰어 호출 없이 하위 목록, 삼성화재 표 186개 절 2.1s.
   - `from=` 은 `start=` 로 — MCP SDK 가 템플릿 변수와 함수 인자 이름을 맞추는데 `from` 은 파이썬 예약어.
   - 창은 줄 끝에서 자른다(상한의 90~100% 구간) — 표 행이 반 토막 나지 않게.
   - 알려진 한계: rowspan 이 있는 다단 머리 표는 머리 행이 여러 줄로 펼쳐진다(`_table_to_markdown` 재사용, colspan 만 처리). 원문은 보존되므로 읽는 쪽이 판단한다.
   - **본문 출처 결정(260906): 뷰어(A).** document.xml 을 `<TITLE>` 순서로 자르는 B 도 되고(4건 순서 일치, 표본 18절 숫자 24,750/24,754 공통, 표·행 수 동일) 절당 0초지만, XML 이 23~27M 자라 로드 시 RSS 가 금융사 +200MB — 1GB VM 에서 둘 겹치면 260901 OOM 모양이라 보류. 실측·판단은 storage `wiki-private/lessons/filing-section-source-choice-260906.md`.
3. ✅ 도구 → 절 주소: `filing_sections.origin_hint()` 한 함수로 `business_details`(머리·저신뢰 NOT_FOUND) · `financial_notes`(읽은 절 주소 + 실패 필드 목차) · `asset_holdings`(extraction_failed·cross_reference) · `shareholder_meeting_notice`(약한 파싱 경고). 뷰어 추가 호출 0. `tests/test_tool_output_section_hints.py` 5건.
4. ⬜ CLAUDE.md 규칙 2(뷰어는 service 명시분만)·7(병렬 표현) 문구 — 사용자가 직접 정한다. 다 풀리면 이 문서는 삭제하고 durable 한 부분은 `decisions/` 로 옮긴다(handoff README 생명주기).

## 관련
`scripts/probe_viewer_sections.py`(계측) · `open_proxy_mcp/resources.py` · `open_proxy_mcp/dart/client.py:1914-2020` · [[mcp-endpoints]] · [[business_details]] · [[financial_notes]]
